https://app.gizmo.ai/*

Required so the packaged MAIN-world content script can be registered for that origin. Chrome's scripting.registerContentScripts rejects the registration without a matching host permission. The extension does not call fetch. Gizmo's page loads its own scripts. The content script only changes in-memory reads after those modules initialize.
